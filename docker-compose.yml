version: '3'

services:

  llm-backend:
    build:
      context: ./Backend
    ports:
      - "3001:3001"

  llm-fontend:
    build:
      context: .
    ports:
      - "5173:5173"
    depends_on:
      - llm-backend